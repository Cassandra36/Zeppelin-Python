# Zeppelin-Python
Took the original script for a couple different platforms and combined them together creating a page that users can select from a drop down or checkboxes to pull the data. Instead of having to comment out each line that was not needed. 

Using Pyspark combined two different major platforms in order to pull data on one notebook instead of two different notebooks. Will work on adding more platforms that we use as time progresses.
